<?xml version="1.0" encoding="utf-8"?>
<android.support.v7.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/settings_card_view"
    android:layout_height="40dp"
    android:layout_width="match_parent">
    <RelativeLayout
        android:id="@+id/loot_notification_switch_div"
        android:layout_height="match_parent"
        android:layout_width="match_parent">

        <!-- we need to find the image for this-->
        <ImageView
            android:id="@+id/settings_card_img"
            android:contentDescription="@string/nothing"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content" />

        <TextView
            android:id="@+id/settings_card_textview"
            android:layout_centerHorizontal="true"
            android:layout_centerVertical="true"
            android:layout_height="wrap_content"
            android:layout_width="wrap_content"
            android:text="@string/loot_notifications"
            android:textSize="15sp"/>
    </RelativeLayout>
</android.support.v7.widget.CardView>