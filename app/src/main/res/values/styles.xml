<resources>

    <!-- Base application theme. -->
    <style name="themeButton" parent="Widget.AppCompat.Button">
        <item name="android:textAllCaps">false</item>
        <item name="android:textStyle"></item>
    </style>
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/themeRed</item>
        <item name="colorPrimaryDark">@color/darkGray</item>
        <item name="colorAccent">@color/themeRed</item>
        <item name="android:fontFamily">roboto</item>
        <item name="android:color">@color/themeRed</item>
        <item name="android:textAppearanceButton">@style/themeButton</item>
    </style>
    <style name="SplashTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@drawable/splash_drawable</item>
    </style>

    <style  name="AppThemeActionBar"
            parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/themeRed</item>
        <item name="colorPrimaryDark">@color/darkGray</item>
        <item name="colorAccent">@color/themeRed</item>
        <item name="android:actionMenuTextColor">@color/white</item>
        <item name="android:fontFamily">roboto</item>
        <item name="android:color">@color/themeRed</item>
        <item name="android:textAppearanceButton">@style/themeButton</item>
    </style>

</resources>
